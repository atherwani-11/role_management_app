# Replace these values with your Railway MySQL connection details for production.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=role_management_app

# Render provides PORT automatically in production, but you can override it locally.
PORT=8081
